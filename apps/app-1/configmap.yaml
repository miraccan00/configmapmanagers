apiVersion: v1
kind: ConfigMap
metadata:
  name: example-config
  namespace: app-1
data:
  # Key-value pairs for environment variables or configuration files
  APP_NAME: "MyApp"
  APP_ENV: "production"
  LOG_LEVEL: "debug"
  DB_HOST: "mysql.example.com"
  DB_PORT: "3306"